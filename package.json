{
  "name": "cypress-log-to-term",
  "version": "0.0.0-development",
  "description": "Overwrite the cy.log command to print to both the Command Log and to the terminal",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "files": [
    "src",
    "commands.js"
  ],
  "scripts": {
    "test": "cypress run",
    "semantic-release": "semantic-release",
    "lint": "tsc --noEmit --pretty",
    "stop-only": "stop-only --folder cypress/e2e"
  },
  "keywords": [
    "cypress-plugin"
  ],
  "author": "Gleb Bahmutov <gleb.bahmutov@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^12.5.1",
    "prettier": "^2.8.3",
    "semantic-release": "^20.0.0",
    "stop-only": "^3.3.1",
    "typescript": "^4.9.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bahmutov/cypress-log-to-term.git"
  },
  "dependencies": {
    "safe-stable-stringify": "^2.4.2",
    "string-format": "^2.0.0"
  }
}
